// Getting extra props from Examples.jsx with ...props and passing it to the actual section below
export default function Section({ title, children, ...props }) {
  return (
    <section {...props}>
      <h2>{title}</h2>
      {children}
    </section>
  );
}
